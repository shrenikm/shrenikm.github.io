---
title: "On Conditioning Generative Policies"
date: "2026-05-31T21:32:31-08:00"
tags: ["experiment", "generative_policies", "learnings", "machine_learning"]
draft: false
---

Gotta fill my post quota before the rest of the year zips past, so here we go. This _probably_ won't be my last post of the year, but more on that another day.

I've been running some experiments on conditioning diffusion and flow matching policies. This writeup will cover some of my learnings.

## Objective

I started working on this to figure out what the limits of FiLM conditionning were. I'd never really completely wrapped my head around FiLM's effectiveness. A lot of the time it felt like things worked out with FiLM even though it shouldn't have? This then grew into wanting to know how other conditioning methods stack against each other. With a lot of VLAs switching over to using flow matching action heads these days, I ended up adding this into the mix as well.

I started my experiments trying to validate a few hypothesis:

1. FiLM and other similar methods apply a sort of global conditioning across all timesteps equally. In principle these _should_ break at a certain complexity of task conditioning
2. Something like cross-attention _should_ in principle be better for tasks that require high fidelity local conditioning
3. A lot of these methods will probably fail for more complex compositional conditioning. Maybe there's some conditioning method out there that can more efficiently encode this?

Obviously none of this really matters if you're using an LLM/VLM backbone for your policy. So we limit our discussions and experiments to smaller models (< 50M) that don't employ such backbones (think diffusion policy).

While none of my hypothesis are particularly imaginative or controversial, there were still a few open questions (at least open to me):
1. At what point does FiLM break?
2. How does FiLM stack against something more primitive?
3. How does FiLM stack against something slight more spruced up?
4. Do these methods perform differently for diffusion and flow matching?

To test all of these, a few things are required:
1. A toy dataset of actions/trajectories that is easy to synthesize and work with
2. Implementations of diffusion and flow matching models that can generate said actions/trajectories
3. Collection of conditioning methods to compare


### Dataset

I generated two families of synthetic datasets, both of which are collections of `T=64` $(x, y)$ points bounded in 2-D space. The obvious question is how this scales to more realistic scenarios where we end up with a larger space of actions that includes angles, etc. Honestly, I don't know. We could have more complex inter-point feasible constraints, etc for actual robot actions and the results here might not carry over. However, I don't think that the representation problem differ a lot in terms of complexity. Angles can always be represented with $(\cos \theta, \sin \theta)$, rotations with 6-D representations, and so on. If the model has enough capacity, it should be able to just figure it out. All that to say, I'd be surprised if most things from this 2-D experiment didn't translate in some form to more practical problems.

For `Dataset1`, we sample trajectories from four different classes: 

<Table
  headers={["Class", "Fraction", "Parameterization", "Distribution"]}
  rows={[
    [
      "Straight line",
      "0.20",
      "length $l$, angle $\\theta$",
      "$l \\in U(0.6, 1.6)$, $\\theta \\in U(0, 2\\pi)$",
    ],
    [
      "Constant curvature (Dubins path)",
      "0.20",
      "radius $r$, sweep $\\alpha$, sign $s$",
      "$r \\in U(0.3, 0.9)$, $\\alpha \\in U(\\pi/3, 3\\pi/2)$, $s \\in \\pm 1$",
    ],
    [
      "L-shaped",
      "0.20",
      "line + Dubins arc",
      "Line and Dubins arc distributions",
    ],
    [
      "S-shaped",
      "0.20",
      "two Dubins arcs, opposite signs",
      "Dubins arc distribution",
    ],
    [
      "Spiral (Clothoid)",
      "0.20",
      "length $l$, start curvature $\\kappa_s$, curvature rate $\\kappa_r$",
      "$l \\in U(2.5, 4.5)$, $\\kappa_s \\in \\pm U(0.5, 2.5)$, $\\kappa_r \\in \\pm U(0.5, 2.5)$",
    ],
  ]}
/>

The dataset contains 500,000 trajectories, sampled according to the distributions above. Note that some of the boundaries between the classes are intentionally blurry (Eg: L-shaped and S-shaped trajectories can look quite similar, etc.) to ensure that some of the trivial conditional tasks aren't too easy. Here's a gallery of some randomly sampled trajectories:

<ZoomableFigure src="/posts/10/images/datasets/dataset1_gallery.png" alt="Dataset1 gallery" />

For `Dataset2`, we generate trajectories in an occupancy map to simulate 2-D path planning. The map is a 64x64 occupancy grid with circular obstacles sampled in the following way:

<Table
  headers={["Tier", "Fraction", "Obstacle count", "Obstacle radius"]}
  rows={[
    ["Sparse", "0.20", "$U(1, 6)$", "$U(0.08, 0.20)$"],
    ["Medium", "0.35", "$U(7, 21)$", "$U(0.06, 0.18)$"],
    ["Dense", "0.45", "$U(22, 45)$", "$U(0.05, 0.16)$"],
  ]}
/>

This dataset contains 200,000 trajectories computed by sampling random start and end positions and computing the shortest path between them using A*.

<ZoomableFigure src="/posts/10/images/datasets/dataset2_gallery.png" alt="Dataset1 gallery" />


### Generative Policies

For the policies, we use a 1-D U-Net structure (as seen in the original Diffusion Policy). Each model and conditioning variant uses FiLM for conditioning the timestep (even if the other inputs aren't conditioned through FiLM). The base model (base U-Net + timestep FiLM) has ~`10M` parameters.

For diffusion, we train the model to predict the noise $\epsilon$. For the samples, we use DDPM (`T=100` with a cosine schedule). For inference we use DDIM with the `25-50` steps depending on the experiment.

For flow matching, we use the same model but predict the velocity instead. During sampling, we sample $t \sim \beta(1.5, 1)$, and use `10-20` Euler steps during inference.

<ZoomableFigure src="/posts/10/images/architecture/unet_architecture.png" alt="Dataset1 gallery" />


### Conditioning Methods


#### FiLM

For FiLM, an MLP produces per-channel scale $\gamma$ and shift $\beta$. The inputs get modulated as $\gamma \cdot x + \beta$ broadcast over all `T` actions to produce the `(B, C, T)` output.

#### Concat

I wanted to test something that's a bit more primitive. Concat basically does what it advertises: it concatenates the conditioning feature vector to the input channels (broadcast across `T`). The `(B, C+cond_dim, T)` tensor is then projected into `(B, C, T)`.

#### AdaGN

This one is supposed to be the dressed up version of FiLM. AdaLN-Zero has become popular in DiT networks, but given that we're using 1-D convolutions here, we stick to the group norm. The output is $(1 + \gamma) \cdot \texttt{GroupNorm}(x) + \beta$, with it starting zero initialized similar to AdaLN-Zero.

#### Cross-Attention

The only method that applies per timestep conditioning. Queries come from the action trajectory (one per `T`) with the keys/values coming from the condition tokens. We use multi-head attention with 4 heads, with FFN expansions stacked on top for some of the experiments. For single scalar/vector conditioning, this collapses into a single token attention module.


## Experiments

Each section here will start with tables outlining the training hyperparameters and model sizes used in the experiment.

### Sanity Check


<Table caption="Training hyperparameters"
  headers={["Policy", "Steps", "Batch", "LR", "NFE"]}
  rows={[
    ["Diffusion", "20k/80k", "256", "1e-4", "25"],
    ["Flow Matching", "20k/80k", "256", "1e-4", "10"],
  ]}
/>

<Table caption="Model size (M)"
  headers={["FiLM", "AdaGN", "Concat", "Cross-attn"]}
  rows={[
    ["9.844", "9.844", "9.844", "9.844"],
  ]}
/>

The first thing to do is to make sure that our base unconditional models work as expected. Training the models on just the spiral trajectories from `Dataset1` gives the following output samples:

<Gallery
  images={[
    { src: "/posts/10/images/sanity_check/sanity_check1_diffusion.png", alt: "Sanity Check - Diffusion", caption: "Diffusion (Spiral)" },
    { src: "/posts/10/images/sanity_check/sanity_check1_flow_matching.png", alt: "Sanity Check - Flow Matching", caption: "Flow Matching (Spiral)" },
  ]}
/>

And no, I couldn't resisting making the classic denoising gifs.


<Gallery
  images={[
    { src: "/posts/10/gifs/sanity_check/sanity_check1_diffusion.gif", alt: "Sanity Check - Diffusion", caption: "Diffusion (Spiral)" },
    { src: "/posts/10/gifs/sanity_check/sanity_check1_flow_matching.gif", alt: "Sanity Check - Flow Matching", caption: "Flow Matching (Spiral)" },
  ]}
/>

Training unconditionally on all of `Dataset1` gives you samples from every class as expected.

<Gallery
  images={[
    { src: "/posts/10/images/sanity_check/sanity_check2_diffusion.png", alt: "Sanity Check - Diffusion", caption: "Diffusion (All)" },
    { src: "/posts/10/images/sanity_check/sanity_check2_flow_matching.png", alt: "Sanity Check - Flow Matching", caption: "Flow Matching (All)" },
  ]}
/>

### Experiment 1

<Table caption="Training hyperparameters"
  headers={["Policy", "Steps", "Batch", "LR", "NFE"]}
  rows={[
    ["Diffusion", "40k", "256", "1e-4", "25"],
    ["Flow Matching", "40k", "256", "1e-4", "10"],
  ]}
/>

<Table caption="Model size (M)"
  headers={["FiLM", "AdaGN", "Concat", "Cross-attn"]}
  rows={[
    ["9.872", "9.872", "8.918", "10.167"],
  ]}
/>

We start with something, very simple: conditioning on the trajectory class (in `Dataset1`). We want to ensure two things:
1. The models are able to produce trajectories of the given class
2. The models exhibit diversity within trajectories of the same class

The first thing here isn't trivial to verify. To do this we train a small 1-D convolution classifier. The classifier has an `~100%` on a generated validation dataset, so we can use it to gauge the accuracy of conditioning.

<Table
  caption="Class accuracy (256 samples/condition)"
  align={["left", "right", "right"]}
  headers={["", "Diffusion", "Flow Matching"]}
  rows={[
    ["FiLM", "0.988", "0.985"],
    ["AdaGN", "0.995", "0.980"],
    ["Concat", "0.991", "0.986"],
    ["Cross-attn", "0.987", "0.993"],
  ]}
/>

The accuracies aren't `1.0` but given that the boundaries between the classes aren't that distinct, we'll give the models a pass. Unsurprisingly, we don't see any differences betwen the conditioning mechanisms or the models for this task.

We can also measure the `diversity` (pairwise Euclidean distance between trajectories) of the samples within each class. These turn out to be pretty good at `~7.5-8.0`, with the values being smaller for the line trajectories and larger for curves and arcs. Samples for each combination:

<Gallery
  images={[
    { src: "/posts/10/images/exp1/exp1_diffusion_film.png", alt: "Exp1, Diffusion, FiLM ", caption: "Diffusion + FiLM" },
    { src: "/posts/10/images/exp1/exp1_diffusion_adagn.png", alt: "Exp1, Diffusion, AdaGN ", caption: "Diffusion + AdaGN" },
    { src: "/posts/10/images/exp1/exp1_diffusion_concat.png", alt: "Exp1, Diffusion, Concat ", caption: "Diffusion + Concat" },
    { src: "/posts/10/images/exp1/exp1_diffusion_ca.png", alt: "Exp1, Diffusion, Cross-attn ", caption: "Diffusion + Cross-attn" },
    { src: "/posts/10/images/exp1/exp1_flow_matching_film.png", alt: "Exp1, Flow Matching, FiLM ", caption: "Flow Matching + FiLM" },
    { src: "/posts/10/images/exp1/exp1_flow_matching_adagn.png", alt: "Exp1, Flow Matching, AdaGN ", caption: "Flow Matching + AdaGN" },
    { src: "/posts/10/images/exp1/exp1_flow_matching_concat.png", alt: "Exp1, Flow Matching, Concat ", caption: "Flow Matching + Concat" },
    { src: "/posts/10/images/exp1/exp1_flow_matching_ca.png", alt: "Exp1, Flow Matching, Cross-attn ", caption: "Flow Matching + Cross-attn" },
  ]}
/>

### Experiment 2

<Table caption="Training hyperparameters"
  headers={["Policy", "Steps", "Batch", "LR", "NFE"]}
  rows={[
    ["Diffusion", "40k", "256", "1e-4", "25"],
    ["Flow Matching", "40k", "256", "1e-4", "10"],
  ]}
/>

<Table caption="Model size (M)"
  headers={["FiLM", "AdaGN", "Concat", "Cross-attn"]}
  rows={[
    ["9.890", "9.890", "8.927", "10.184"],
  ]}
/>


<Table
  caption="Class accuracy, L2 (256 samples/condition) — higher is better; all within binomial noise"
  align={["left", "right", "right"]}
  headers={["", "Diffusion", "Flow Matching"]}
  rows={[
    ["FiLM", "0.966", "0.982"],
    ["AdaGN", "0.973", "0.972"],
    ["Concat", "0.966", "0.980"],
    ["Cross-attn", "0.963", "0.968"],
  ]}
/>






